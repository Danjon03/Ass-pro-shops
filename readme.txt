FOR THE FIRST TIME EVER:
- create a folder for the project
- open that folder in the terminal and run the following commands

npm init -y
npm install @hapi/hapi
npm install @hapi/inert
npm install path

TO RUN THE SERVER:
navigate to project folder in terminal and type "node server.js"
